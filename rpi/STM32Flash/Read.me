This directory contains the STM32Flash utility for updating the firmware.

To build it:
1) cd STM32Flash
2) make
3) make install

This code was not written by ADRCS, so we cannot provide support for it. Use as is.
